import 'package:flutter/material.dart';

class AppColors {
  static const white = Color(0xFFFFFFFF);

  // Brand
  static const primary = Color(0xFFE31E36);
  static const primaryDark = Color(0xFFC40026);
  static const primaryDarker = Color(0xFF570909);
  static const secondary = Color(0xFF000000);

  static const crimsonRed = Color(0xFFE30031);
  static const lightRed = Color(0xFFFFE5E5);
  static const green = Color.fromARGB(255, 23, 173, 78);

  // Semantic
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFFACC15);
  static const error = Color(0xFFEF4444);

  // Text
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);

  // Background
  static const background = Color(0xFFF9FAFB);

  // Greys
  static const surfaceGrey = Color(0xFFF5F5F5);
  static const grey = Color(0xFF9CA3AF); // Middle grey
  static const silver = Color(0xFFC0C0C0);
  static const lightGrey = Color(0xFFD1D5DB);
  static const darkGrey = Color(0xFF4B5563); // Dark grey
}
